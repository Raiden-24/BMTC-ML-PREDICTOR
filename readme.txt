✅ Prerequisites
First, make sure Python is installed (preferably version 3.8+).


----> Very imp change the path of your DATASET.csv file in both python files before execution <----

---> You can check with: <--- 
python --version

If not installed, download from: https://www.python.org/downloads/

---> Then install pip (Python’s package manager, usually comes with Python).

💡 One-Liner to Install Everything at Once:

---> You can copy and paste this in your terminal or command prompt: <---
pip install pandas numpy plotly dash scikit-learn catboost

---> Install virtualenv: <---
pip install virtualenv

---> Create and activate: <---
virtualenv venv
venv\Scripts\activate
 

or

pip install -r requirements.txt
